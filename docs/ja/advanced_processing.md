# 高度な処理

特別な場面のためのオプション機能です。ここにあるものはすべて GUI
（対応する設定を探してください。それぞれにツールチップがあります）と
CLI の両方で使えます。

## ノイズ除去

復元された領域にはノイズが残ることがあります。ノイズ除去の設定
（`--denoise low|medium|high`）は、復元された領域だけに穏やかな空間
ノイズ除去を適用します — フレームのそれ以外の部分はそのままです。
まず `low` から始め、ノイズが残る場合だけ上げてください。

デフォルトではセカンダリ復元の前に実行されます。
`--denoise-step after_secondary` を指定すると、合成の直前に移動します。

## 検出の安定化フィルタ

検出はフレーム単位では完璧ではありません。モザイクが 1〜2 フレームだけ
消えると 1 つのクリップが複数に分断され（つなぎ目が目立ち、未処理フレーム
が残る）、単発の誤検出は不要な復元を引き起こします。

- **最大検出ギャップ**（`--max-detection-gap`、デフォルト `2`）：モザイクが
  同じ位置に再出現する場合、最大 N フレームの途切れを補完してクリップを
  連続させます。
- **最短検出フレーム数**（`--min-detection-duration`、デフォルト `2`）：
  N フレーム未満の検出を誤検出として破棄します。該当フレームは未処理の
  ままになります。

本当に素早く現れて消える場面に影響しないよう、どちらも小さい値に保って
ください。`0` で無効になります。

## シーンカット検出

この機能がない場合、ハードカット（シーンの切り替わり）をまたいで追跡された
モザイクが 1 つのクリップに含まれ、復元時にカット前後の映像が混ざってしまう
ことがあります。**シーンカット検出**（`--scene-detection`、デフォルトはオン）
はハードカットを検出し、その位置で追跡中のクリップをすべて終了するため、
各クリップは単一のショット内に収まります。GPU 上で動作し、コストはごくわずか
です。

実際にはカットがないのにクリップが分割される場合のみ、`--no-scene-detection`
（または GUI の詳細設定セクションのスイッチ）で無効化してください。

## 60 FPS から 30 FPS への書き出し

60（または 59.94）FPS の入力では、**60 FPS を 30 FPS に変換**
（`--retarget-high-fps`）が 1 フレームおきに処理して 30（または 29.97）
FPS で出力します — 処理量は半分になります。音声のタイミングと再生速度は
維持されます。他のフレームレートは変更されません:

```bash
jasna --input input.mp4 --output output.mp4 --retarget-high-fps
```

[区間処理](segments.md)とは併用できません。

## 処理中でも再生可能（フラグメント化 MP4）

通常の MP4 は処理が終わるまで開けません。**処理中でも再生可能**
（`--fmp4`）にすると、作成中の出力をそのまま再生できます — 待たずに品質を
確認でき、途中で中断してもファイルは再生可能なままです:

```bash
jasna --input input.mp4 --output output.mp4 --fmp4
```

映像は数秒ずつ増えていき、処理が終わるまでプレーヤーの再生時間表示が正しく
ないことがあります。影響するのは `.mp4` と `.mov` の出力だけです。ストリー
ミングと[区間処理](segments.md)では使用できません。

## カラー LUT

`.cube` カラー LUT（1D または 3D）を出力に適用できます — カラー
グレーディングや、決まったルックに合わせるためです。GUI のエンコード
セクションか、`--lut path/to/look.cube` で設定します。LUT はエンコード
直前に GPU で適用されるため、コストはほとんどかかりません。

## シャープネス

復元した映像は少し柔らかく見えることがあります。GUI のエンコードセクションの
**シャープネス**（`--sharpen`）は、書き出しながら輪郭や細部をくっきりさせます。
別のツールで再エンコードする必要はありません。

```bash
jasna --input in.mp4 --output out.mkv --sharpen 0.5
```

`0` で無効、`0.2`〜`0.5` は控えめな効果、`1` は最も強く、不自然に見えることが
あります。シャープにするとファイルが大きくなるため、結果が悪化する場合は CQ 値も
下げてください。プレビューには反映されません。

## 字幕の焼き込み

Jasna はエンコード中に `.ass`/`.ssa` 字幕を映像へ焼き込めるため、ハードサブ版の
書き出しに ffmpeg での再エンコードは不要です。GUI のエンコードセクションの
**字幕を焼き込む**で設定するか、`--burn-subtitles` を使います:

```bash
# 1 本の動画に 1 つの字幕ファイル
jasna --input in.mp4 --output out.mkv --burn-subtitles subs.ass

# フォルダー全体: 各動画は隣にある video.ass（または video.ssa）を使う
jasna --input folder_in --output folder_out --burn-subtitles auto
```

`auto` の場合、同名字幕のない動画は字幕なしで書き出され、ログにその旨が
記録されます。テキストは同梱の `ffmpeg`（libass）が、この PC にインストール
されたフォントで描画します。未インストールのフォントが必要なら
`--subtitle-fonts-dir` でフォントフォルダーを追加してください。タイミングは
プレイヤーのタイムラインに従うため、元動画に合わせて作った字幕は 60→30 FPS
書き出しでもずれません。テキストはカラー LUT の後、シャープネスの前に GPU で
合成され、描画は補助プロセスが数フレーム先行して行うため、1080p では負荷が
小さく、フレームサイズに応じて増えます。

字幕の焼き込みは全フレームを再エンコードするため、[区間処理](segments.md)とは
併用できず、ストリーミングには適用されず、プレビューにも表示されません。元の
ファイルに含まれる字幕ストリームは従来どおりコピーされます。

## エンコーダー品質とカスタム設定

エンコーダー品質には GUI の **CQ** コントロールまたは `--cq` を使います。
表示または入力した数値は変更せずエンコーダーへ渡され、低いほど高品質で
ファイルが大きくなります。Jasna は出力サイズも制限するため、上限に達すると
近い CQ 値で同じ結果になる場合があります:

```bash
jasna --input in.mp4 --output out.mkv --cq 22
```

**カスタム引数**フィールド（`--encoder-settings`）は、ビットレート上限や
キーフレーム間隔など、その他の高度な設定に使います。CQ コントロールの数値と
エンコーダーが受け取る数値を常に一致させるため、GUI のカスタム引数では CQ の
別名を受け付けません。各コーデックのキー、実値範囲、デフォルトは
[CLI リファレンス](cli.md)に記載されています。

## エクスポート後のアクション

キュー全体が完了したときに何かを実行できます: **PC をシャットダウン**、
または**カスタムコマンド**（たとえば通知スクリプト）です。GUI の
エクスポート後のアクションセクションか、CLI で設定します:

```bash
jasna --input input.mp4 --output output.mkv --post-export-action shutdown
jasna --input folder_in --output folder_out --post-export-action command --post-export-command "echo done"
```

正常に書き出された各動画の後にコマンドを実行するには、**動画ごとのコマンド**を
入力するか、`--post-export-video-command` を使います。Jasna はコマンドの完了を
待機し、失敗した場合はその動画をエラーにします。パスのプレースホルダーは
自動的に引用符で保護されます:

```bash
jasna --input folder_in --output folder_out --post-export-video-command "ffmpeg -i {output} -map 0 -map_metadata 0 -map_chapters 0 -c copy -movflags +faststart {output_dir}/{output_stem}_remuxed{output_suffix}"
```

使用できるプレースホルダーは `{input}`、`{output}`、`{output_dir}`、
`{output_stem}`、`{output_suffix}` です。この例では元の出力を残します。
安全に置き換える場合は一時ファイルを使うスクリプトを指定してください。
`ffmpeg` は `PATH` に追加するか、フルパスで指定します。コマンドの作業フォルダーは
出力フォルダーです。
